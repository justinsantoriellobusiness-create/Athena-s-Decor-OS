[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages_latest.pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "NODE_ENV=production node dist/index.js"
